I want a Glance dashboard for selfhosted services that includes quick utility status, richer feed content, and a clean mobile fallback. Choose the right upstream page recipe before building the YAML, make each column feel intentional on its own, and only use `head-widgets`, `group`, or `split-column` where the chosen recipe really benefits from them.
