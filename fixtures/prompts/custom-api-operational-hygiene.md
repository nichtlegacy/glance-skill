Build a Glance `custom-api` widget for a self-hosted service where the URL, token, and one optional query parameter must stay configurable. Keep the widget copy-pasteable, use good secret hygiene, account for Glance-specific operational concerns such as caching, reloads, and query parameter handling, and render it with a real upstream row, list, or card family.
