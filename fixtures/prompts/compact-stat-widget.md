Build a compact Glance `custom-api` widget for a small side column that shows 2-3 key service metrics. Make it explicitly fit a `small` slot using a centered utility block or compact row pattern, prefer the standard shell unless frameless is clearly justified, and avoid oversized cards or decorative layout that belongs in a full-width section.
