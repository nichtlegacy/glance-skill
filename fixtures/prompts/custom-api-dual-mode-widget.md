Build a reusable Glance `custom-api` widget that should work both in a narrow side column and in a full-width content column. Expose the display differences through `options` instead of maintaining two separate templates, choose an upstream family that can adapt cleanly across both slots, and keep the result Glance-native and copy-pasteable.
