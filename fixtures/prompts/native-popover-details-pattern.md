Create a Glance widget that shows a compact list but still exposes secondary details on demand. Use a real native list or row family, keep the always-visible surface tight for its target slot, and use native Glance disclosure patterns such as `data-popover-*`, `<details>`, collapse helpers, or relative-time helpers where appropriate. Do not invent custom JavaScript-like interaction.
